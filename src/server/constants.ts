/** Stable identity for the dev/local login, shared by the seed and auth config. */
export const DEV_USER = {
  id: "dev-user",
  email: "dev@blobgen.local",
  name: "Dev User",
  image: "/images/motivation.jpg",
} as const;
